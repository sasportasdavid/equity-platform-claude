// Module 3b — La state machine awards vit désormais dans
// `apps/web/src/lib/stateMachines/awardStateMachine.ts` (call-site Server
// Actions). Le type `AwardStatus` + Zod schema sont dans
// `packages/shared/src/schemas/award.ts` (source de vérité partagée).
//
// Ce fichier reste comme placeholder pour de futures state machines
// partagées entre apps (par ex. validation client-side du plan_status).
export {};
